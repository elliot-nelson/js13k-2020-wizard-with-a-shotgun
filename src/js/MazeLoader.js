
export const MazeLoader = {
    load() {
        return
        ;
    }
};

/*

        return {
            maze,
            walls: this.createWalls(maze, roomLookup),
            tiles: this.createTiles(maze, rand),
            rand,
            rooms: roomLookup,
            flowhome: homeflow
        };
        */
